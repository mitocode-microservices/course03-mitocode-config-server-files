server:
  port: ${CUSTOM.SERVER.PORT:9004}

eureka:
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

encrypt:
  key: mitocode
